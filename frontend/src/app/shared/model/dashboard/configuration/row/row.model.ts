import { RowConfiguration } from './row.configuration';

export class Row {

  configuration: RowConfiguration;

  constructor(configuration: RowConfiguration) {
    this.configuration = configuration;
  }

}
