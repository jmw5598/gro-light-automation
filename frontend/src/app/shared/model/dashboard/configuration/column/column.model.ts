import { ColumnConfiguration } from './column.configuration';
export class Column {

  configuration: ColumnConfiguration;
  

  constructor(configuration: ColumnConfiguration) {
    this.configuration = configuration;
  }

}
