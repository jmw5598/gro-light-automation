import { KeyExtractPipe } from './key-extract.pipe';

describe('KeyExtractPipe', () => {
  it('create an instance', () => {
    const pipe = new KeyExtractPipe();
    expect(pipe).toBeTruthy();
  });
});
