import { PanelMenuDirective } from './panel-menu.directive';

describe('PanelMenuDirective', () => {
  it('should create an instance', () => {
    //const directive = new PanelMenuDirective(private element: ElementRef, private renderer : Renderer);
    //expect(directive).toBeTruthy();
  });
});
