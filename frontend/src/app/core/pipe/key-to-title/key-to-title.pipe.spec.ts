import { KeyToTitlePipe } from './key-to-title.pipe';

describe('KeyToTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new KeyToTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
