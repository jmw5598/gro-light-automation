import { NotificationInfiniteScrollDirective } from './notification-infinite-scroll.directive';

describe('NotificationInfiniteScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new NotificationInfiniteScrollDirective();
    expect(directive).toBeTruthy();
  });
});
