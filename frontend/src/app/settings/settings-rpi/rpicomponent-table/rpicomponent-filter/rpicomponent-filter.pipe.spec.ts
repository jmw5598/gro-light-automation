import { RPiComponentFilterPipe } from './rpicomponent-filter.pipe';

describe('RPiComponentFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RPiComponentFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
